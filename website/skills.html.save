<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SKILLS EXAMS - CPE 212 Automating Server Management</title>
    <style>
        body { 
            margin: 0; 
            font-family: Helvetica, Arial, sans-serif; 
            background-color: #d9d9d9; 
        }
        header { 
            background-color: #f39b5c; 
            color: #222; 
            padding: 20px 38px 8px 38px; 
            font-weight: bold; 
            font-size: 1.55em; 
            position: relative; 
        }
        .back-btn { 
            position: absolute; 
            top: 15px; right: 38px; 
            background: #fff; 
            color: #f39b5c; 
            border: none;
            font-size: 1.2em; 
            font-weight: bold; 
            padding: 6px 22px; 
            border-radius: 22px; 
            cursor: pointer;
            box-shadow: 0 2px 6px rgba(0,0,0,0.06); 
            transition: background 0.2s, color 0.2s; 
        }
        .back-btn:hover { 
            background: #f39b5c; 
            color: #fff; 
        }
        .container { 
            display: flex; 
            height: calc(100vh - 60px); 
        }
        .sidebar { 
            background: #ff7f2a; 
            width: 360px; 
            display: flex; 
            flex-direction: column; 
            justify-content: flex-start; 
            align-items: flex-start; 
            padding-top: 75px; 
            font-size: 1.3em; 
            font-weight: bold; 
            color: black; 
        }
        .sidebar button { 
            background: none; 
            border: none; 
            color: black; 
            font-size: 0.9em; 
            font-weight: bold; 
            margin-bottom: 28px; 
            margin-left: 24px; 
            cursor: pointer; 
            text-align: left; 
            width: 90%; 
            transition: color 0.2s; 
        }
        .sidebar button:hover { 
            color: #fff; 
        }
        .sidebar button.active-btn { 
            background-color: #ad4c00 !important; 
            color: #fff !important; 
        }
        .main-content { 
            flex: 1; 
            background: #e6e6e6; 
            display: flex; 
            flex-direction: column; 
            align-items: center; 
            padding-top: 65px; 
        }
        .hoas-title { 
            font-size: 4em; 
            font-weight: bold; color: #222; 
        }
        .orange-divider { 
            margin: 0 auto 24px auto;
            width: 95%; 
            max-width: 870px; 
            height: 8px; 
            background: #ff7f2a; 
            border-radius: 6px; 
        }
        .pdf-preview-box { 
            background: #fff; 
            border-radius: 32px; 
            width: 95%; 
            max-width: 1200px; 
            height: 400px; 
            min-height: 220px; 
 
